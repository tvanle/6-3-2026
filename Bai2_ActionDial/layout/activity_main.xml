<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout Bài 2 - Giao diện nhập số điện thoại
    - EditText nhập số (inputType="phone" để hiện bàn phím số)
    - ImageButton icon điện thoại để quay số
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Quay Số Điện Thoại"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="32dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- inputType="phone" hiển thị bàn phím số điện thoại -->
        <EditText
            android:id="@+id/edtPhoneNumber"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Nhập số điện thoại"
            android:inputType="phone"
            android:textSize="18sp" />

        <!-- ImageButton dùng icon điện thoại hệ thống -->
        <ImageButton
            android:id="@+id/btnDial"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:src="@android:drawable/sym_action_call"
            android:contentDescription="Gọi điện"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:layout_marginStart="8dp" />

    </LinearLayout>

</LinearLayout>
