<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml - Bài 3
    - Permission READ_MEDIA_IMAGES cho Android 13+ (API 33+)
    - Permission READ_EXTERNAL_STORAGE cho Android 12 trở xuống
    - maxSdkVersion="32" giới hạn permission cũ chỉ cho Android ≤12
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bai3_actionpick">

    <!-- Android 13+ (API 33): dùng READ_MEDIA_IMAGES thay cho READ_EXTERNAL_STORAGE -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Android 12 trở xuống: dùng READ_EXTERNAL_STORAGE -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Bai3 - Image Picker"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
