<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml - Bài 4
    - Permission CAMERA để sử dụng camera
    - <uses-feature> khai báo app cần camera
      (required="false" để app vẫn cài được trên thiết bị không có camera)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bai4_actionimagecapture">

    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Khai báo app sử dụng camera, nhưng không bắt buộc -->
    <uses-feature android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Bai4 - Camera"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
